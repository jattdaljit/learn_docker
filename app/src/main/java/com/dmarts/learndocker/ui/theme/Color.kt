package com.dmarts.learndocker.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Background / Surface (Light) ─────────────────────────────────────────
val NeoBackground     = Color(0xFFF5F7FF)
val NeoSurface        = Color(0xFFFFFFFF)
val NeoSurfaceVariant = Color(0xFFEEF2FF)
val NeoBorder         = Color(0xFFDDE3F0)

// ─── Primary — Docker Blue ─────────────────────────────────────────────────
val NeoCyan           = Color(0xFF2563EB)
val NeoCyanDim        = Color(0xFF1D4ED8)
val NeoCyanContainer  = Color(0xFFDBEAFE)

// ─── Success / Running — Green ─────────────────────────────────────────────
val NeoGreen          = Color(0xFF16A34A)
val NeoGreenDim       = Color(0xFF15803D)

// ─── Secondary — Violet ────────────────────────────────────────────────────
val NeoPurple         = Color(0xFF7C3AED)
val NeoPurpleDim      = Color(0xFF5B21B6)
val NeoPurpleContainer = Color(0xFFEDE9FE)

// ─── Status ────────────────────────────────────────────────────────────────
val NeoAmber          = Color(0xFFD97706)
val NeoAmberDim       = Color(0xFFB45309)
val NeoRed            = Color(0xFFDC2626)
val NeoRedDim         = Color(0xFFB91C1C)

// ─── Text ──────────────────────────────────────────────────────────────────
val NeoTextPrimary    = Color(0xFF111827)
val NeoTextSecondary  = Color(0xFF6B7280)
val NeoTextMuted      = Color(0xFFD1D5DB)

// ─── Terminal (stays dark for readability) ─────────────────────────────────
val NeoTerminalBg     = Color(0xFF1A1D2E)
val NeoTerminalText   = Color(0xFFE2E8F0)
val NeoTerminalError  = Color(0xFFF87171)
val NeoTerminalInput  = Color(0xFF93C5FD)
val NeoTerminalSystem = Color(0xFFFCD34D)
val NeoTerminalSuccess = Color(0xFF6EE7B7)
val NeoTerminalExplain = Color(0xFF60A5FA)

// ─── Syntax highlighting (terminal input) ──────────────────────────────────
val SyntaxCommand     = Color(0xFF7DD3FC)
val SyntaxSubcmd      = Color(0xFF6EE7B7)
val SyntaxFlag        = Color(0xFFFBBF24)
val SyntaxValue       = Color(0xFFE2E8F0)
val SyntaxPort        = Color(0xFFC084FC)
val SyntaxImage       = Color(0xFFFDA4AF)

// ─── Container status ──────────────────────────────────────────────────────
val ContainerRunning  = NeoGreen
val ContainerStopped  = Color(0xFF9CA3AF)
val ContainerPaused   = NeoAmber

// ─── Story speaker colors ──────────────────────────────────────────────────
val CipherColor       = NeoCyan
val NexusColor        = NeoPurple
val NovaColor         = NeoAmber
val SystemColor       = NeoGreen

// ─── XP / Achievement ─────────────────────────────────────────────────────
val XpGold            = Color(0xFFF59E0B)
val XpSilver          = Color(0xFF9CA3AF)
val XpBronze          = Color(0xFFCD7F32)
